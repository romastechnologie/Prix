<form method="post" action="{{ path('app_conditionnement_delete', {'id': conditionnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conditionnement.id) }}">
    <button class="btn">Delete</button>
</form>
