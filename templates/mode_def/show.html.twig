{% extends 'base.html.twig' %}

{% block title %}ModeDef{% endblock %}
{% block pageActive %} Mode de définition des prixs {% endblock %}

{% block content %}
    <h1>ModeDef</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_def.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ mode_def.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mode_def_index') }}">back to list</a>

    <a href="{{ path('app_mode_def_edit', {'id': mode_def.id}) }}">edit</a>

    {{ include('mode_def/_delete_form.html.twig') }}
{% endblock %}
