{% extends 'base.html.twig' %}

{% block title %}Mot de passe{% endblock %}
{% block pageActive %} Mot de passe  {% endblock %}
{% block content %}
 {{ form_start(form) }}
    <div class="card card-custom" id="kt_card_2">
	<div class="card-header">
		<div class="card-title">
			<h3 class="card-label">RECHERCHE</h3>
		</div>
		<div class="card-toolbar">
			<button type="submit" class="btn btn-warning"   title="Modification de l'utilisateur">
			<i class="ki ki-reload icon-nm"></i>Modifier
			</button>
		</div>
	</div>
	<div class="card-body">
       
            {{ include('registration/reg/_formpassword.html.twig', {'button_label': 'Update'}) }}
            <div class="card-footer">
            <button type="submit" class="btn btn-primary"   title="Modification de l'utilisateur">
                <i class="ki ki-reload icon-nm"></i>Modifier
            </button>
            <a type="button" href="{{ path('app_utilisateur_index') }}" class="btn btn-warning">Retour</a>
            </div>

	</div>
</div>
        {{ form_end(form) }}
   
{% endblock %}
